"""
Sextant Orbital Resilience Framework
Mission Control Dashboard Layer

Provides a structured, human-readable operational view of:
- constellation health
- cascade impacts
- mission timeline events
- system risk state

This is a simulation dashboard layer (no UI framework dependency).
"""

class MissionDashboard:
    def __init__(self, runtime, scenario_engine):
        self.runtime = runtime
        self.scenario_engine = scenario_engine

    def render_constellation_state(self):
        """
        Displays current orbital system state.
        """
        state = self.runtime.system.get_system_state()

        print("\n🛰️ CONSTELLATION STATE")
        print("======================")

        for node_id, data in state.items():
            print(f"{node_id} | {data['type']} | {data['status']}")

    def render_cascade_snapshot(self):
        """
        Displays failure propagation summary.
        """
        impact = self.runtime.cascade.get_cascade_impact()

        print("\n💥 CASCADE SNAPSHOT")
        print("===================")

        print(f"Failed Nodes   : {impact['failed']}")
        print(f"Degraded Nodes : {impact['degraded']}")
        print(f"Nominal Nodes  : {impact['nominal']}")

    def render_mission_report(self):
        """
        Displays AI + governance combined mission interpretation.
        """
        report = self.runtime.governance.evaluate_mission_state()

        print("\n🧠 MISSION REPORT")
        print("=================")
        print(report["report"])

        print("\n⚖️ GOVERNANCE DECISION")
        print("======================")
        print(report["governance_decision"])

    def render_scenario_timeline(self):
        """
        Displays scenario execution history over time.
        """
        log = self.scenario_engine.get_scenario_log()

        print("\n⏱️ SCENARIO TIMELINE")
        print("====================")

        for entry in log:
            print(f"t={entry['time']} | {entry['event']} | decision={entry['evaluation']['governance_decision']}")

    def full_dashboard(self):
        """
        Complete mission control view.
        """
        self.render_constellation_state()
        self.render_cascade_snapshot()
        self.render_mission_report()
        self.render_scenario_timeline()
